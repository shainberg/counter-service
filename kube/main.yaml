apiVersion: v1
kind: Service
metadata:
  name: my-cntsvc-svc
  labels:
    app: cntsvc
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: cntsvc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-cntsvc
  labels:
    app: cntsvc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cntsvc
  template:
    metadata:
      labels:
        app: cntsvc
    spec:
      containers:
      - name: cntsvc
        image: 630943284793.dkr.ecr.eu-central-1.amazonaws.com/counter-service:latest
        ports:
        - containerPort: 80
        imagePullPolicy: Always
